{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd Weather/frontend && npm install",
  "buildCommand": "cd Weather/frontend && npm run build",
  "outputDirectory": "Weather/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}